## Prerequisites

- Node.js
- The following environment variables are set: `API_URL`, `API_KEY`, `INDEX_NAME`.

## Install dependencies

```bash
npm install
```

## Run the script

```bash
node quickstart.js
```